#include <stdio.h>

int main()
{
    /* teste do git */
    int x;
    int y;
    printf("Digite o valor de x:");
    scanf("%d",&x);
    printf("Digite o valor de y:");
    scanf("%d",&y); /* Recebemos os valores de x e y */
  
    int soma;
    soma = x+y; /*Cria uma variável inteira que guarda a soma de x e y */
  
    printf("A soma de x e y é: %d", soma);
  
  return 0;
  
}
